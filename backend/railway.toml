# railway.toml (TOML, not JSON)

# Optional: keep the schema as a quoted key (or just comment it out)
"$schema" = "https://railway.com/railway.schema.json"

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1"
