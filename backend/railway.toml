# railway.toml (TOML, not JSON)

#"$schema" = "https://railway.com/railway.schema.json"
#
#[build]
#builder = "DOCKERFILE"
#
#[deploy]
#startCommand = "uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1"


#"$schema" = "https://railway.com/railway.schema.json"
#
#[build]
#builder = "DOCKERFILE"
#
#[deploy]
#startCommand = "sh -c 'uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8080} --workers 1'"

#"$schema" = "https://railway.com/railway.schema.json"
#
#[build]
#builder = "DOCKERFILE"
#
#[deploy]
#startCommand = "sh -c 'uvicorn main:app --host 0.0.0.0 --port ${PORT:-8080} --workers 1 --log-level info'"

####
"$schema" = "https://railway.com/railway.schema.json"

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8080} --workers 1 --log-level info'"

